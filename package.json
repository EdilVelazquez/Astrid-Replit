{
  "name": "deployment-wrapper",
  "private": true,
  "scripts": {
    "build": "cd project && npm install && npm run build",
    "postinstall": "cd project && npm install"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.89.0",
    "cors": "^2.8.5",
    "express": "^5.2.1"
  }
}
